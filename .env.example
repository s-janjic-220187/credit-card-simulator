# Environment variables template for Railway deployment
# Copy this file and set the appropriate values in Railway dashboard

# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/creditcard_simulator

# JWT Secret (generate a strong random string)
JWT_SECRET=your_jwt_secret_key_here_generate_a_strong_random_string

# CORS Configuration
CORS_ORIGIN=https://your-frontend-domain.railway.app

# Backend URL (set this in frontend environment)
VITE_API_BASE_URL=https://your-backend-domain.railway.app

# PostgreSQL Password
POSTGRES_PASSWORD=your_secure_postgres_password
